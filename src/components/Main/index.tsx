import { Slider } from "../Slider"

export const Main = () => {
    return (
        <section>
            <div className="container">
                <Slider />
            </div>
        </section>
    )
}

