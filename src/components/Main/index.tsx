import { Slider } from "../Slider"

export const Main:React.FC = () => {
    return (
        <section>
            <div className="container">
                <Slider />
            </div>
        </section>
    )
}

